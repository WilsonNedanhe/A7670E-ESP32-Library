---
name: Bug report
about: Something in the library doesn't behave as documented
labels: bug
---

**Module** (e.g. `SMS`, `ATClient`, `UART`):

**Board / A7670E carrier board:**

**PlatformIO / library version:** (see `A7670E_LIB_VERSION`)

**What happened:**

**What you expected:**

**Minimal sketch that reproduces it:**
```cpp

```

**Serial log** (set `A7670E_DEBUG=1` in your build flags for `>>`/`<<` AT traffic):
```

```
